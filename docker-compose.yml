services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: campusai
      POSTGRES_USER: campusai
      POSTGRES_PASSWORD: campusai_dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
